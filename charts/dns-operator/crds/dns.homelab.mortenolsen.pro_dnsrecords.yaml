---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: dnsrecords.dns.homelab.mortenolsen.pro
spec:
  group: dns.homelab.mortenolsen.pro
  names:
    kind: DNSRecord
    listKind: DNSRecordList
    plural: dnsrecords
    singular: dnsrecord
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DNSRecord is the Schema for the dnsrecords API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DNSRecord
            properties:
              description:
                description: Description is an optional description/notes
                type: string
              dnsClassRef:
                description: DNSClassRef references the DNSClass to use
                properties:
                  name:
                    description: Name is the name of the DNSClass
                    type: string
                required:
                - name
                type: object
              domain:
                description: Domain is the domain name (e.g., "example.com")
                type: string
              metadata:
                description: |-
                  Metadata contains record-specific metadata
                  For MX records: priority
                  For SRV records: priority, weight, port
                properties:
                  port:
                    description: Port is used for SRV records
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  priority:
                    description: Priority is used for MX and SRV records
                    format: int32
                    minimum: 0
                    type: integer
                  weight:
                    description: Weight is used for SRV records
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              subdomain:
                description: Subdomain is the subdomain (use "@" for root domain,
                  or "www" for www.example.com)
                type: string
              ttl:
                description: TTL overrides the DNSClass default TTL
                format: int32
                minimum: 1
                type: integer
              type:
                description: Type is the DNS record type (A, CNAME, AAAA, TXT, MX,
                  SRV, NS)
                enum:
                - A
                - AAAA
                - CNAME
                - TXT
                - MX
                - SRV
                - NS
                type: string
              values:
                description: |-
                  Values are the record value(s)
                  For A records: IPv4 addresses (array for multiple values)
                  For CNAME: single canonical name
                  For AAAA: IPv6 addresses (array)
                  For TXT: text strings (array)
                items:
                  type: string
                minItems: 1
                type: array
            required:
            - dnsClassRef
            - domain
            - subdomain
            - type
            - values
            type: object
          status:
            description: status defines the observed state of DNSRecord
            properties:
              conditions:
                description: Conditions represent the current state of the DNSRecord
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              error:
                description: Error contains error details if any
                properties:
                  code:
                    description: Code is the error code
                    type: string
                  message:
                    description: Message is the error message
                    type: string
                  timestamp:
                    description: Timestamp is when the error occurred
                    format: date-time
                    type: string
                type: object
              fqdn:
                description: FQDN is the full DNS name (domain + subdomain)
                type: string
              lastSyncTime:
                description: LastSyncTime is the timestamp of the last sync
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed DNSRecord
                format: int64
                type: integer
              state:
                description: State is the current state of the record
                enum:
                - Created
                - Updated
                - Deleted
                - Error
                - Pending
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
