---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: dnsclasses.dns.homelab.mortenolsen.pro
spec:
  group: dns.homelab.mortenolsen.pro
  names:
    kind: DNSClass
    listKind: DNSClassList
    plural: dnsclasses
    singular: dnsclass
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DNSClass is the Schema for the dnsclasses API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DNSClass
            properties:
              defaultTTL:
                description: DefaultTTL is the default TTL for records using this
                  DNSClass
                format: int32
                minimum: 1
                type: integer
              hmacAuth:
                description: HMACAuth configures HMAC-based authentication for request
                  integrity verification
                properties:
                  algorithm:
                    default: SHA256
                    description: 'Algorithm specifies the HMAC algorithm to use (default:
                      SHA256)'
                    enum:
                    - SHA256
                    - SHA512
                    type: string
                  secret:
                    description: Secret is a direct secret value (less secure, for
                      testing only)
                    type: string
                  secretRef:
                    description: SecretRef references a Kubernetes Secret containing
                      the HMAC shared secret
                    properties:
                      key:
                        description: Key is the key within the Secret containing the
                          shared secret
                        type: string
                      name:
                        description: Name is the name of the Secret
                        type: string
                      namespace:
                        description: Namespace is the namespace containing the Secret
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                type: object
              server:
                description: Server is the URL for the DNS provider webhook service
                pattern: ^https?://.+
                type: string
              timeoutSeconds:
                description: TimeoutSeconds is the connection timeout in seconds
                format: int32
                minimum: 1
                type: integer
            required:
            - server
            type: object
          status:
            description: status defines the observed state of DNSClass
            properties:
              conditions:
                description: Conditions represent the current state of the DNSClass
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              health:
                description: Health represents the server health status (informational,
                  does not block operations)
                properties:
                  lastCheck:
                    description: LastCheck is the timestamp of the last health check
                    format: date-time
                    type: string
                  message:
                    description: Message is an optional health check message
                    type: string
                  status:
                    description: Status is the health status
                    enum:
                    - Healthy
                    - Unhealthy
                    - Unknown
                    type: string
                type: object
              lastHealthCheck:
                description: LastHealthCheck is the timestamp of the last health check
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed DNSClass
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
